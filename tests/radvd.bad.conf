# Intentionally malformed radvd config: tests/smoke.sh asserts that radvd's
# configtest mode (-c) rejects it (missing closing brace / unknown directive).
interface eth0 {
    AdvSendAdvert on
    ThisDirectiveDoesNotExist yes;

# Minimal valid radvd config used only by tests/smoke.sh to exercise radvd's
# configtest mode (-c). Not a runtime config.
interface eth0 {
    AdvSendAdvert on;
    IgnoreIfMissing on;
    prefix 2001:db8::/64 {
        AdvOnLink on;
        AdvAutonomous on;
    };
};
